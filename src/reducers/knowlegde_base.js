import * as Actions from './../constants/ActionTypes';

let defaultState = [];

const knowlegde_base = (state=defaultState, action) => {
    switch (action.type) {
        default: return [...state];
    }
};

export default knowlegde_base;
